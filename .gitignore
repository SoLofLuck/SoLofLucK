# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# check-claim-abi.mjs opens a temporary folder here to build and run the client
# code (it has to be under node_modules: the compiled module imports
# @solana/web3.js). It normally cleans up after itself; if a run is interrupted
# the leftovers can remain.
node_modules/.claim-abi-*
